form.form.form-horizontal.col-md-12(name='recoverForm', ng-show='isEnabledView("recover")')
  p.text-center Read email inbox for instructions
  .form-group
    span.col-md-3.control-label Email
    .col-md-9
      input.form-control(type='email', ng-model='recover.email'
        required='required', placeholder='Your Email')
  .form-group
    .col-md-9.col-md-offset-3
      .row
        a.btn.btn-primary.col-md-12.col-xs-12(ng-click='doRecover()',
          ng-disabled='recoverForm.$error.email || !recover.email') Recover Account
    .col-md-9.col-md-offset-3
      br
      .col-md-6
        a.pointer(ng-click='enableView("login")') Log in
